services:
  app:
    image: ${IMAGE:?IMAGE must be set}
    container_name: fieldbook-app
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DOC_ASSET_DIR: /app/data/doc-assets
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - ./files:/app/data/doc-assets
